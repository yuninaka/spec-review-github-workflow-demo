# spec-review-github-workflow-demo

「[個人実証(番外編)：型分離PoCを作ろうとして、作らなくてよいと気づくまで](https://zenn.dev/yuninaka/articles/type-separation-spec-review-retrospective)」で提案した、仕様書レビューをGitHubの標準機能(プルリクエスト・ブランチ保護・Actions)で組み立てる運用案を、実際に動かして検証するためのデモリポジトリ。

## このリポジトリで検証していること

記事内の「GitHubを使う場合の運用案」で挙げた対応関係を、実際の設定・操作で確認している。

| 記事内の設計 | GitHubの実際の機能 | このリポジトリでの検証 |
|---|---|---|
| `DraftSpecification` / `ApprovedSpecification`の型による区別 | プルリクエスト上の変更 / 保護されたブランチ(`main`)にマージされた状態 | `main`ブランチにブランチ保護ルールを設定し、プルリクエスト経由でのみ変更できるようにしている |
| 承認記録の永続化 | プルリクエストのレビュー承認 | 実際のプルリクエストで承認を行い、承認者・承認日時が記録されることを確認している(各PRのReviewsを参照) |
| 「承認後に仕様書が更新されたら再レビューが必要」 | ブランチ保護ルールの「新しいコミットがpushされたら既存の承認を無効化する」設定 | 承認済みのプルリクエストに追加のコミットをpushし、承認が無効化されることを確認している |
| 差分の可視化 | プルリクエストの差分表示 | 各プルリクエストのFiles changedタブで確認できる |
| 残った独自ロジック(仕様書の内容チェック) | プルリクエストに対して自動実行されるチェック | `.github/workflows/spec-check.yml`が、仕様書に必須セクションが揃っているかを確認し、結果をプルリクエストにコメントする |

## 構成

- `spec/current-spec.md`: 「承認済み仕様書」に相当するダミーファイル。内容は検証用の架空のものであり、実在のプロジェクト・企業とは一切関係ない
- `.github/workflows/spec-check.yml`: プルリクエストに対して自動実行されるチェック。仕様書に必須セクション(対象範囲・変更内容・確認事項)が揃っているかを確認し、結果をコメントする

## 注意

これは概念実証のためのデモであり、実運用を想定したものではない。仕様書の内容チェックロジックも、記事で検討した「構造化データの矛盾チェック」や「固定プロンプトによる生成AIの確認」の簡易な代替として、必須セクションの有無を確認するだけの最小限の実装にとどめている。
